var modeSurvival = {
    name: 'survival',
    roundTimeSeconds: 30,
    playerMaxHealth: 10,
    resourcesPerRound: 1,
    mapSize: 5
};

// make this available to our users in our Node applications
module.exports = modeSurvival;
